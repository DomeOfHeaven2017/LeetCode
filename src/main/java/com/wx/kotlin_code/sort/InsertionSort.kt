package com.wx.kotlin_code.sort

/**
 * 插入排序算法
 */
class InsertionSort<T> : BaseSort(){

    fun insertionSort(array: Array<T>) {
        show("排序前：", array)

        for (i in array.indices) {
            for (j in i..0 ) {

            }
        }



        show("排序后：", array)
    }
}